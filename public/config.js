// Optional runtime overrides for native / late-bound deploys.
// Vercel production typically uses VITE_* baked at build — leave this empty.
// Capacitor OTA can rewrite this file without resubmitting the IPA.
window.__MINEOPS_CONFIG__ = window.__MINEOPS_CONFIG__ || {}
